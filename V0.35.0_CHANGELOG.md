# v0.35.0 — CCTV Region + Smart Layout + Faster Playback

## CCTV 地區校正
- CCTV 不再直接把 `全台國道`、`全台省道`、`Taiwan` 當成縣市。
- 優先使用鏡頭名稱內的明確縣市，其次使用地方政府來源；只有缺少明確行政區時才以座標做保守縣市推定。
- API 回傳新增 `regionResolvedBy` / `regionConfidence`，方便後續 QA 找出仍需人工校正的鏡頭。
- 台北 101、板橋、宜蘭市、台中、台南、高雄等基準點已加入回歸測試。

## 地圖 CCTV 不互擋
- 搜尋地點後的 LIVE CCTV 卡片改為螢幕座標碰撞偵測。
- 卡片會在鏡頭周圍的上／下／左／右候選位置自動錯開，並盡量維持在地圖可視範圍內。
- 地圖縮放、移動、視窗尺寸改變後會重新排版，不重新載入影像。
- 手機仍限制最多 2–3 張，桌機最多 4 張，避免視野再次被 CCTV 蓋滿。

## 點擊直接大畫面
- 地圖 CCTV 小畫面點擊後直接開啟中央可拖曳 CCTV Popup。
- 不再先把情報抽屜撐開，也不因點 CCTV 而重新 flyTo 地圖。
- Popup 仍可關閉、拖曳、啟用 LIVE ANALYSIS。

## 播放速度與自動播放
- `.m3u8/.jpg/.mjpeg/.mp4/.webm` 等明確格式直接走播放器，不再先做一次多餘的媒體探測。
- 其他不明格式的探測逾時縮短；公開索引鏡頭失敗時更快切回 in-app 附近 CCTV 備援。
- 媒體解析快取延長至 30 分鐘，減少同一鏡頭重複解析。
- Video/HLS 強制 muted + playsinline，並於 metadata/canplay 事件再次嘗試 autoplay，提高 Safari / iOS 成功率。
- 地圖預覽採 fast path，最近鏡頭優先載入並錯峰啟動。
