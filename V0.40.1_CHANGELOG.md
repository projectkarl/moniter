# EYE // TAIWAN v0.40.1 — CCTV Visible First

- Kept v0.40 freeway FLOW rendering unchanged.
- Nearby CCTV fast phase now prioritizes the most relevant local-government sources instead of waiting for freeway/highway sources in the first response.
- Added a second background official-road CCTV enrichment pass with a longer timeout budget.
- Map CCTV previews now include official position-only cameras instead of hiding every camera without an immediately playable stream.
- Position-only CCTV cards are labeled `POINT`; clicking them opens source/location details rather than failing silently.
- Last known official registry data remains usable during a temporary upstream refresh timeout within the same warm server instance.
- Service worker cache advanced to v401.
